/**
* @author Lianna Eeftinck / https://github.com/Leeft
*/

import THREE from 'three';

const generateUUID  = THREE.Math.generateUUID;
const degToRad      = THREE.Math.degToRad;
const radToDeg      = THREE.Math.radToDeg;

export {
  generateUUID,
  degToRad,
  radToDeg,
};
